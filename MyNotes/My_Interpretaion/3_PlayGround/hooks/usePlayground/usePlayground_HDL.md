HLD (High-Level Design)

1. 🎯 Purpose

- usePlayground is a custom React hook that:
    - Loads playground data (project info + file structure) from the backend.
    - Manages loading, error, and success states.
    - Allows saving updated template data back to the backend.
    - Displays toast notifications for success/error feedback.

